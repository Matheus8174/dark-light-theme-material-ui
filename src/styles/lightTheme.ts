import type { ThemeOptions } from '@mui/material/styles';

const theme: ThemeOptions = {
  palette: {
    mode: 'light',
    background: {
      default: '#fafbfc',
    },
    secondary: {
      main: '#27272a',
    }
  },
};

export default theme;