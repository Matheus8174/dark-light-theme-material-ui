import type { ThemeOptions } from '@mui/material/styles';

const theme: ThemeOptions = {
  palette: {
    mode: 'dark',
    background: {
      default: '#222222',
    },
    secondary: {
      main: '#fafbfc',
    },
  },
};

export default theme;